# Normalize to LF in the repo. The deploy shell scripts run on Linux (nikam) —
# CRLF would break the shebang / bash. Force LF for everything text.
* text=auto eol=lf

*.png binary
*.jpg binary
*.svg text eol=lf
